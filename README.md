 # Basic Thread Creation & Wait ## 
Description: A C++ program using the Windows API (`CreateThread`) to spawn multiple threads based on a command-line argument. It uses `WaitForMultipleObjects` to block the main thread until all worker threads have completed their execution. 
